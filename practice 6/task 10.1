arr = input("enter the array: ")
list_new = list(map(float, arr.split()))

absence = True
for i in range(len(list_new)):
    for l in range(i+1,len(list_new)):
        if list_new[i] == list_new[l]:
            absence = False
            print("dubl:",list_new[i])

if absence == True:
    print("no dubl")
